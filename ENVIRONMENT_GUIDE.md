# 🛠️ AFTT0822 환경 자동화 가이드

폴더를 바꿀 때마다 발생하는 환경 문제를 자동으로 해결하는 스크립트들입니다.

## 📋 제공되는 스크립트들

### 1. 🚀 빠른 시작 (권장)
```bash
./quick-start.sh
```
- **가장 많이 사용할 명령어**
- 환경을 자동으로 체크하고 문제가 있으면 자동 수정
- 개발 서버를 바로 실행
- **매번 이것만 실행하면 됩니다!**

### 2. 🔧 환경 설정 및 체크
```bash
./dev-setup.sh
```
- 환경 상태를 자세히 체크
- node_modules, package-lock.json 상태 확인
- 보안 취약점 자동 수정
- Next.js 빌드 테스트

### 3. 🛠️ 강력한 환경 복구
```bash
./fix-env.sh
```
- **환경이 완전히 꼬였을 때 사용**
- 모든 것을 삭제하고 처음부터 재설치
- npm 캐시도 완전 정리
- 마지막 수단으로 사용

## 🎯 사용법 (간단!)

### 새 폴더에서 처음 작업할 때:
```bash
cd /path/to/your/project
./quick-start.sh
```

### 환경 문제가 발생했을 때:
```bash
./fix-env.sh
```

### 평상시 개발할 때:
```bash
./quick-start.sh
```

## 🔍 문제 해결

### "Permission denied" 오류가 나면:
```bash
chmod +x *.sh
./quick-start.sh
```

### 스크립트가 없다면:
이 파일들이 프로젝트 루트에 있는지 확인하세요:
- `quick-start.sh`
- `dev-setup.sh` 
- `fix-env.sh`

### 여전히 문제가 있다면:
1. `./fix-env.sh` 실행 (강력한 복구)
2. Node.js 버전 확인: `node -v`
3. npm 버전 확인: `npm -v`

## ⚡ 핵심 장점

✅ **더 이상 수동으로 환경 설정할 필요 없음**
✅ **폴더 바꿔도 문제없음**
✅ **자동으로 모든 문제 해결**
✅ **한 번의 명령어로 모든 것 해결**

## 💡 팁

- **평상시**: `./quick-start.sh`만 사용하세요
- **문제 발생시**: `./fix-env.sh`로 한 번에 해결
- 모든 스크립트는 색상으로 상태를 알려줍니다
- Ctrl+C로 언제든 중단 가능

---

**🎉 이제 환경 문제로 스트레스받지 마세요!**



